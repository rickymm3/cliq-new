-# app/views/posts/_form.html.haml

-# If you are passing `post` and `cliq` as locals, 
-# just ensure they are defined above this partial call.
-# Also ensure `current_user` is available if referencing `user_signed_in?`.
= form_with model: post, local: true, class: "mb-3" do |form|
  .mb-3
    = form.label :title, class: "form-label"
    = form.text_field :title, class: "form-control", placeholder: "Enter the title of your post"

  .mb-3
    = form.label :content, class: "form-label"
    = form.rich_text_area :content, class: "form-control content-textarea", placeholder: "Write your post here..."

  = form.hidden_field :user_id, value: current_user.id if user_signed_in?
  = form.hidden_field :cliq_id, value: cliq.id if cliq

  .d-grid.gap-2
    = form.submit 'Submit', class: "btn btn-primary btn-lg"
